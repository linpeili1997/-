import Vue from 'vue'
import { Button, Checkbox, Form, FormItem, Input, Menu, MenuItem, MenuItemGroup, Submenu, } from 'element-ui'

Vue.use(Button).use(Input).use(Form).use(FormItem).use(Checkbox).use(Menu).use(MenuItem).use(MenuItemGroup).use(Submenu);
